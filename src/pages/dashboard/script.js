(() => { const session=window.YOGDPS?.getSession?.(); const projects=window.YOGDPS?.getProjects?.()||[]; document.querySelector('#user-name').textContent=session?.name||'Creator'; document.querySelector('#project-count').textContent=projects.length; const list=document.querySelector('#project-list'); if(!projects.length){list.innerHTML='<div class="empty-state">Você ainda não possui um GDPS.<br><a href="../builder/index.html" style="color:var(--primary-light)">Crie seu primeiro projeto →</a></div>';return;} list.innerHTML=projects.map(p=>`<article class="project-card"><h3>${escapeHtml(p.name)}</h3><p>${escapeHtml(p.slug)}</p><span class="status">● Configuração local</span></article>`).join(''); document.querySelector('#logout').addEventListener('click',()=>{localStorage.removeItem('yogdps_session');location.href='../auth/index.html';}); function escapeHtml(v){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));} })();
