(() => { const root=document.querySelector('#projects'); const projects=window.YOGDPS?.getProjects?.()||[]; if(!projects.length){root.innerHTML='<div class="empty-projects">Nenhum GDPS criado ainda.<br><br><a class="button button-primary" href="../builder/index.html">Criar primeiro GDPS →</a></div>';return;} root.innerHTML=projects.map((p,i)=>`<article class="project-item"><div class="project-icon">Y</div><h2>${safe(p.name)}</h2><p>${safe(p.slug)} · ${safe(p.version||'Core')}</p><div class="project-actions"><a class="mini-button" href="../dashboard/index.html">Abrir painel</a><button class="mini-button" data-delete="${i}">Excluir</button></div></article>`).join(''); root.querySelectorAll('[data-delete]').forEach(btn=>btn.addEventListener('click',()=>{const next=window.YOGDPS.getProjects();next.splice(Number(btn.dataset.delete),1);window.YOGDPS.saveProjects(next);location.reload();})); function safe(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]);} })();
